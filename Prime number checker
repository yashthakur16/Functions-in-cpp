#include<iostream>
using namespace std;

bool isprime(int n)
{
    for (int i=2;i<n;i++)
    {
        if(n%i==0)
        {
            return 1;
        }
    }

    return 0;
}

int main ()
{
    int n;
    cout<<"Enter the number = ";
    cin>>n;
    if(isprime(n)==0)
    {
        cout<<"Prime ";
    }
    else
    {
        cout<<"Not Prime ";

    }
}
